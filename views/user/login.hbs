<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Poppins">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-image: url('/product-images/background.jpg');
      background-size: cover;
      background-position: center center;
      background-repeat: repeat-y;
      
      
    }

    .form-border {
      max-width: 400px;
      margin: 40px auto;
      padding: 20px;
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow:50px 0px 50px 50px rgba(0, 0, 0, 0.5);
      opacity: 0.9;
     
    }

    .form-border h2 {
      font-weight: bolder;
      color: #0b0230;
      text-align: center;
      margin-bottom: 30px;
    }

    .form-group label {
      font-weight: bolder;
    }

    .form-control {
      border: 1px solid #ddd;
      border-radius: 5px;
      padding: 10px;
    }

    .form-text {
      color: #5c5757;
    }

    .btn-info {
      background-color: #074f6e;
      color: #ffffff;
      border: none;
      font-weight: bold;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      width: 100%;
    }

    .btn-info:hover {
      background-color: #ffffff;
      border: 1px solid #100386;
      color: #1400c9;
    }

    .signup-link {
      display: block;
      text-align: center;
      margin-top: 15px;
      color: #5c5757;
      font-weight: bold;
    }
  

    /* Responsive adjustments */
    @media (max-width: 768px) {
      .form-border {
        margin: 20px;
        padding: 15px;
      }
    }
  </style>
</head>
<body>
  <div style="align-items: center; margin-left: 25%;margin-top: 10%;">
    <h4 style="color: #ffffff; font-weight: bold;" > Wellcome Back!</h4>
  <img src="/product-images/onclick.png" style="width: 170px; padding-bottom: 10px;" alt="">
  </div>
  <div class="form-border">
    <div class="container">
      <h2>Login</h2>
      <form action="/login" method="post" enctype="multipart/form-data">
        {{#if userLoginErr}}
        <p class="error" style="color: red;">{{userLoginErr}}</p>
        {{/if}}
        <div class="form-group">
          <label for="exampleInputEmail1">Email address</label>
          <input type="email" class="form-control" name="Email" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email">
          <small id="emailHelp" class="form-text">We'll never share your email and password with anyone else.</small>
        </div>
        <div class="form-group">
          <label for="exampleInputPassword1">Password</label>
          <input type="password" class="form-control" name="Password" id="exampleInputPassword1" placeholder="Password">
        </div>
        <button type="submit" class="btn btn-info">Login</button>
      </form>
     
      <a href="/signup" class="signup-link">Create a new account?</a>
    </div>
  </div>
</body>
</html>